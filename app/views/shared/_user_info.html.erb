<%= link_to gravatar_for(current_user, size: Settings.gravatar.size_change), current_user %>
<span><%= current_user.name %></span>
<span><%= link_to t("page_user_info.view_my_profile"), current_user %></span>
<span><%= t("page_user_info.other", count: current_user.microposts.count)%></span>
